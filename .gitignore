# Unencrypted source of the landing page — never commit this to the
# public repo. index.html in the repo root is the password-encrypted
# build; source/index.html is the real, editable content.
source/
